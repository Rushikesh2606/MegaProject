<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme updated with your custom colors -->
    <style name="Theme.YourApp" parent="Theme.Material3.DayNight">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryVariant">@color/active_step</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/green</item>
        <item name="colorSecondaryVariant">@color/green</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Surface and background colors -->
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="android:windowBackground">@color/white</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>

    <!-- Custom TabLayout style -->
    <style name="CustomTabLayoutStyle" parent="Widget.Material3.TabLayout">
        <item name="tabIndicatorColor">?attr/colorSecondary</item>
        <item name="tabIndicatorHeight">4dp</item>
        <item name="tabTextColor">?attr/colorOnSurface</item>
        <item name="tabSelectedTextColor">?attr/colorSecondary</item>
        <item name="tabIconTint">?attr/colorOnPrimary</item>
    </style>

    <!-- Custom button style -->
    <style name="CustomContainedButtonStyle" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/green</item>
        <item name="android:textColor">@color/white</item>
        <item name="cornerRadius">8dp</item>
        <item name="strokeWidth">2dp</item>
        <item name="strokeColor">@color/green</item>
    </style>
</resources>
